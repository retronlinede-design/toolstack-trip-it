import { reportUiSummary } from "./reportUiUtils.js";
export function ReportSummary({ trips, fuel, wash }) { const summary = reportUiSummary({ trips, fuel, wash }); const values = [["Trips", summary.trips], ["Legs", summary.legs], ["Distance", `${summary.distance.toFixed(1)} km`], ["Fuel entries", summary.fuel], ["Washes", summary.wash], ["Drivers", summary.drivers], ["Passenger instances", summary.passengers]]; return <section aria-labelledby="report-summary-heading"><h3 id="report-summary-heading" className="text-sm font-semibold text-neutral-800">Report summary</h3><div className="mt-2 grid grid-cols-2 gap-2 sm:grid-cols-4">{values.map(([label, value]) => <div key={label} className="rounded-lg border border-neutral-200 bg-white p-2"><div className="text-[11px] text-neutral-500">{label}</div><div className="font-semibold text-neutral-800">{value}</div></div>)}</div><div className="mt-2 flex flex-wrap gap-2">{Object.entries(summary.fuelTotals).map(([currency, total]) => <span key={currency} className="ts-badge">{total.toFixed(2)} {currency}</span>)}</div></section>; }
