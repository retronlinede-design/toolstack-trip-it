const num = (value) => { const parsed = Number(String(value ?? "").replace(",", ".")); return Number.isFinite(parsed) ? parsed : 0; };
export function groupCurrencyTotals(entries, costField = "totalCost", currencyField = "currency") { return entries.reduce((totals, entry) => { const currency = String(entry[currencyField] || "EUR"); totals[currency] = (totals[currency] || 0) + num(entry[costField]); return totals; }, {}); }
export function fuelLogStats(entries) { const liters = entries.reduce((sum, item) => sum + num(item.liters), 0); const costs = Object.values(groupCurrencyTotals(entries)).reduce((sum, value) => sum + value, 0); return { count: entries.length, liters, currencyTotals: groupCurrencyTotals(entries), latestOdometer: entries.find((item) => item.odometer !== "" && item.odometer != null)?.odometer ?? null, averageCostPerLiter: liters > 0 && Object.keys(groupCurrencyTotals(entries)).length === 1 ? costs / liters : null, fullTankCount: entries.filter((item) => item.fullTank).length }; }
export function washLogStats(entries) { const typeCounts = entries.reduce((counts, item) => ({ ...counts, [item.type || "Other"]: (counts[item.type || "Other"] || 0) + 1 }), {}); return { count: entries.length, currencyTotals: groupCurrencyTotals(entries, "cost"), mostRecent: entries[0]?.date || null, mostUsedType: Object.entries(typeCounts).sort((a, b) => b[1] - a[1] || a[0].localeCompare(b[0]))[0]?.[0] || null }; }
export const fuelDeleteConfirmation = (entry) => `Delete fuel entry from ${entry.date}${entry.station ? ` at ${entry.station}` : ""}?`;
export const washDeleteConfirmation = (entry) => `Delete ${entry.type || "wash"} record from ${entry.date}${entry.location ? ` at ${entry.location}` : ""}?`;
