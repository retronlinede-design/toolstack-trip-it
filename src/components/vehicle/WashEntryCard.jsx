import { VehicleRecordCard } from "./VehicleRecordCard.jsx";
export function WashEntryCard({ entry, onEdit, onDelete }) { return <VehicleRecordCard id={`wash-${entry.id}`} title={entry.type || "Wash"} subtitle={entry.date} facts={[entry.location || "No location", entry.cost !== "" ? `${Number(entry.cost || 0).toFixed(2)} ${entry.currency || "EUR"}` : "No cost", entry.note ? "Note" : ""] .filter(Boolean)} onEdit={() => onEdit(entry)} onDelete={() => onDelete(entry)} editLabel={`Edit ${entry.type || "wash"} from ${entry.date}`} deleteLabel={`Delete ${entry.type || "wash"} from ${entry.date}`} details={<>{entry.note && <div className="whitespace-pre-wrap"><span className="font-medium">Note:</span> {entry.note}</div>}</>} />; }
