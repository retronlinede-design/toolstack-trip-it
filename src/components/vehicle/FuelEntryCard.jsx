import { VehicleRecordCard } from "./VehicleRecordCard.jsx";
export function FuelEntryCard({ entry, onEdit, onDelete }) { return <VehicleRecordCard id={`fuel-${entry.id}`} title={entry.station || "Fuel entry"} subtitle={entry.date} facts={[`${Number(entry.liters || 0).toFixed(2)} L`, `${Number(entry.totalCost || 0).toFixed(2)} ${entry.currency || "EUR"}`, `${entry.odometer || "—"} km`, entry.fullTank ? "Full tank" : "Partial fill", entry.notes ? "Notes" : ""] .filter(Boolean)} onEdit={() => onEdit(entry)} onDelete={() => onDelete(entry)} editLabel={`Edit fuel entry from ${entry.date}`} deleteLabel={`Delete fuel entry from ${entry.date}`} details={<><div><span className="font-medium">Station:</span> {entry.station || "—"}</div>{entry.notes && <div className="whitespace-pre-wrap"><span className="font-medium">Notes:</span> {entry.notes}</div>}</>} />; }
