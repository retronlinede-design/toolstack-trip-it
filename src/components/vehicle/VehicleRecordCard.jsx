import { useState } from "react";
export function VehicleRecordCard({ id, title, subtitle, facts, details, onEdit, onDelete, editLabel, deleteLabel }) { const [expanded, setExpanded] = useState(false); const panelId = `record-${id}`; return <article className="rounded-xl border border-neutral-200 bg-white p-3"><div className="flex items-start justify-between gap-2"><div className="min-w-0"><div className="font-semibold text-neutral-800 break-words">{title}</div><div className="mt-0.5 text-xs text-neutral-500 break-words">{subtitle}</div><div className="mt-1 flex flex-wrap gap-x-3 gap-y-1 text-xs text-neutral-600">{facts.map((fact) => <span key={fact}>{fact}</span>)}</div></div><div className="flex shrink-0 gap-1"><button type="button" className="ts-suggestion-chip" onClick={onEdit} aria-label={editLabel}>Edit</button><button type="button" className="grid h-11 w-11 place-items-center rounded-lg text-neutral-500 hover:bg-neutral-100 focus-visible:ring-2 focus-visible:ring-lime-400" aria-expanded={expanded} aria-controls={panelId} onClick={() => setExpanded((value) => !value)}>{expanded ? "−" : "+"}</button></div></div>{expanded && <div id={panelId} className="mt-3 space-y-2 border-t border-neutral-100 pt-3 text-sm text-neutral-700">{details}<div className="flex justify-end border-t border-neutral-100 pt-3"><button type="button" className="ts-button ts-button--danger" onClick={onDelete} aria-label={deleteLabel}>Delete</button></div></div>}</article>; }
